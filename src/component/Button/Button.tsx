import React from "react";
import { Button, View } from "react-native";

const CustomButton = () => {
  return (
    <View>
      <Button title="test" />
    </View>
  );
};

export default CustomButton;
